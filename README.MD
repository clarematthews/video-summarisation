### Control-chart video summarisation app

#### Requirements

- ffmpeg
- npm
- redis
- Python 3 virtual environment (named `cc`)

#### Running

- In the `client` directory run `npm install`
- Connect to GoPro via wifi
- Run `run.sh` script
